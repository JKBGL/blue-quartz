import React from 'react';

const RoomIndexPopout = (props: { is_creating: boolean }) => {
    // if we are creating or joining a room
    const { is_creating } = props;

    return (
        <div className="room-popout">
            
        </div>
    );

};